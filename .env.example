# Copy to .env and set values (or set in Portainer Stack environment variables).
# Do not commit .env with real credentials.

SQL_SERVER=your-server,port
SQL_DATABASE=weather
SQL_USERNAME=weather
SQL_PASSWORD=your-password
